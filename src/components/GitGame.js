import React from 'react';
import Terminal from "./Terminal";

const GitGame = () => {
    return (
        <div>
            <Terminal message={'>cd'}/>
        </div>
    );
};

export default GitGame;